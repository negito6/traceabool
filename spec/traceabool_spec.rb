require "spec_helper"

describe Traceabool do
  it "has a version number" do
    expect(Traceabool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
